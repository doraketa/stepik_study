#include <iostream>

using namespace std;

int main() {
    
    int inputedValue, nextValue;
    cin >> inputedValue;
    nextValue = (inputedValue / 2 + 1) * 2;
    cout << nextValue;
    return 0;
}